version: "3.9"

services:
  pipeline:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - /tmp/youtube-pipeline:/tmp/youtube-pipeline
    restart: unless-stopped
